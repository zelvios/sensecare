# Where the SvelteKit server reaches the API. Never exposed to the browser.
API_URL=http://localhost:8080
# Lifetime of the login cookie, keep equal to SESSION_TTL_HOURS in the API.
SESSION_TTL_HOURS=12
